# 確認問題を書かせるプロンプト（章ごとに使い回す）

```bash
codex exec --cd "C:/Dev/kikenbutsu_kou_exam_app" --sandbox workspace-write --color never \
  -o out-q-law-what.txt - < scripts/prompts/20-questions.md
```

**教本を書き終えて、通しレビューを反映したあとで走らせてください。**
教本が固まる前に問題を作ると、直した内容と食い違います。

**教本を書いた側とは別の側に書かせてください。**
同じ側に書かせると、教本の誤りがそのまま問題に写ります（引き継ぎ書 §4）。

**このファイルの「---」より下がプロンプト本体です。**「担当章」の 3 行を、そのつど書き換えてください。

## 章ごとの問題数（母集団は本番 45 問の 3 倍＝135 問）

**ちょうど 45 問だと、本番形式の模試が毎回まったく同じ 45 問になります。**
本番の出題数の 3 倍を目安にしてあります。

**科目ごとの合計は公表値の 3 倍と一致させてください**（法令 45 / 物化 30 / **性消 60**）。

| 章 ID | ファイル | 配列名 | 問題数 |
| --- | --- | --- | --- |
| `law-what` | `src/data/questions/law-what.ts` | `lawWhatQuestions` | 9 |
| `law-place` | `src/data/questions/law-place.ts` | `lawPlaceQuestions` | 9 |
| `law-people` | `src/data/questions/law-people.ts` | `lawPeopleQuestions` | 12 |
| `law-rule` | `src/data/questions/law-rule.ts` | `lawRuleQuestions` | 15 |
| `sci-base` | `src/data/questions/sci-base.ts` | `sciBaseQuestions` | 15 |
| `sci-burn` | `src/data/questions/sci-burn.ts` | `sciBurnQuestions` | 9 |
| `sci-stop` | `src/data/questions/sci-stop.ts` | `sciStopQuestions` | 6 |
| `prop-common` | `src/data/questions/prop-common.ts` | `propCommonQuestions` | 6 |
| `prop-1` 〜 `prop-6` | `src/data/questions/prop-1.ts` 〜 `prop-6.ts` | `prop1Questions` 〜 `prop6Questions` | **各 9** |

---

あなたは**危険物取扱者試験 甲種**（一般財団法人消防試験研究センターが実施）の
学習アプリの**確認問題**を書く担当です。

## 担当章

- **章 ID**: `law-what` ← **書き換える**
- **書くファイル**: `src/data/questions/law-what.ts` ← **書き換える**
- **問題数**: **9 問** ← **書き換える**

## まず読むもの

1. `CLAUDE.md` の「問題の書き方」と「系譜で実際に出た誤りの型」 … **必ず最初に読んでください**
2. **`src/data/textbook/<担当章 ID>.ts`** … **担当章の教本本文。問題はここから出します。
   教本に書いていないことを問わないでください**
3. **`docs/primary-numbers.md`** … **数値の一次資料台帳。数字を問う問題は、必ずここの値を使うこと**
4. **`docs/public-questions.md`** … **本番 45 問が何をどう問うたかの分析。
   設問の形の比率と、誤答の作り方をここに合わせてください**
5. `docs/syllabus.md` の担当項目 … 範囲の外に出ないため
6. `src/types.ts` の `Question` … 書き込む型
7. `src/data/questions/index.ts` … 配列名の確認

## 書くファイル

**担当章の 1 ファイルだけを書き換えてください。**現在は空の配列が入っています。
`export const <配列名>: Question[] = [` の形と配列名は**変えないでください。**
**他のファイルは絶対に変更しないでください。**他の章を別の担当が同時に書いています。
**`npm run check` / `npm run build` / `npx tsc --noEmit` を実行しないでください。**
検査は依頼した側がまとめて行います。

## 試験の形（問題の作り方を決める前提）

- **五肢択一式。45 問を 150 分（1 問あたり 3 分 20 秒）。**
  **四肢択一ではありません。選択肢は 5 つです**
- **時間には余裕があります。**読み切るのが精一杯ということはないので、
  **条文を読ませるやや長い問題も成立します**
- **計算は物化だけではありません。**甲種では**法令にも計算が出ます**（指定数量の倍数、防油堤の容量）。
  しかも**試験会場で電卓が使えない**ので、手で解ける形にしてください
- **落とすとしたら数字と、似た制度の取り違えです。**
  指定数量・引火点・保安距離、そして「許可か承認か認可か届出か」

## 1 問の作り方

```ts
{
  id: 'law-what-q01',
  categoryId: 'law-what',
  sectionId: 'lw-3',
  question: '…',
  choices: ['…', '…', '…', '…', '…'],
  answer: 2,
  explanation: '…',
  level: 2,
},
```

- **`id` は `<章 ID>-q01` から連番。**ゼロ詰め 2 桁
- **`sectionId` は必ず付けてください。**解説から教本へ戻る導線と、節ごとの出題に使います。
  **その問題の内容を実際に扱っている節**を指すこと
- **`choices` は 5 要素。**型が 5 要素のタプルなので、4 つだと型エラーになります
- **`answer` は 0〜4**（0 が 1 番目、4 が 5 番目）
- **`level` は体感難易度 1（易）〜3（難）。**1 と 3 を偏らせず、2 を中心に散らしてください
- **`source` は設定しないでください。**公開問題の収録は依頼した側が別に行います

## 守ること

### 問題文

- **空白を除いて 200 字以内。**`npm run check` が 200 字で注意、300 字でエラーにします
- **問題文の中に答えを書かないこと**
- **教本に書かれていることだけを問うこと。**教本にない事項を問わない
- **「法令上」で始める問題を混ぜてください。**本番の法令科目はほとんどがこの形です

### 選択肢

- **5 つ。重複させない**
- **正解の位置を散らすこと。**五肢択一なので、**ならせば各 20 %** です。
  **書き終えたら、0〜4 の数を自分で数えて、報告に書いてください**
- **誤答も正解と同じ密度で書くこと。**
  **正解だけが長いと、読まずに「長いものを選ぶ」で当てられます。**5 つの長さをそろえてください。
  検査は「正解が 24 字以上・最長の誤答の 1.3 倍以上・差 6 字以上」を注意にします
- **「必ず」「すべて」「常に」「一切」を誤答専用にしないこと。**
  言い切りが誤答側にしか出てこないと、それ自体が手掛かりになります。
  **正解の選択肢にも、正しく言い切れる場面では使ってください**
  （例：「移送のためには**常に**危険物取扱者が乗車しなければならない」は正しい言い切りです）
- **誤答は「もっともらしい」ものにすること。**同じ章に出てくる別の値・別の制度を使うのが基本です
  （指定数量の問題の誤答に、他の品名の指定数量を置く、など）
- **分類の軸を混ぜないこと。**5 つの選択肢が、同じ軸の上に並んでいること

### 解説

- **正解の理由だけでなく、誤答がなぜ誤りかにも触れること。**
  **5 つすべてに触れてください。**「誤答の説明もれ」は系譜で最も多い型です
- **数字を訂正する解説では、正しい値を書くこと。**「200 L ではない」で終わらせず「400 L である」まで書く
- **試験テクニックを教えないこと。**
  「『必ず』と言い切った選択肢を疑ってください」のような解説は書かないでください。
  **内容の説明に書き換えること**
- 解説の内容が、**教本の該当節と食い違わないこと**

### 章をまたいだ重複

- **同じことを問う問題を、別の節で作らないこと。**
  `npm run check` が、選択肢まで含めた類似度で検出します
- **担当章の中でも、対比のために対で作る場合を除いて、似た問題を並べないでください**
- **ただし「数値だけ違う問題」は重複ではありません。**
  指定数量の倍数の計算を、値を変えて複数出すのは意図した繰返しです

## 設問の形は本番の比率に寄せる

**本番 45 問の内訳はこうでした**（`docs/public-questions.md`）。

| 形 | 本番 | あなたの担当 |
| --- | --- | --- |
| **誤っているもの / 妥当でないもの / 該当しないもの**を選ぶ | **26 問（58 %）** | **6 割前後に** |
| **正しいもの**を選ぶ（穴埋め・計算を含む） | 19 問（42 %） | 4 割前後 |

**科目ごとに見ると、法令 11/15、物化 4/10、性消 11/20 でした。**
**物化は「正しいものを選ぶ」が多め**になります。

**「正しいものを選ぶ」ばかりにしないでください。**本番と手応えが変わります。

**「誤っているものはどれか」の形では、選択肢 5 つのうち 4 つが正しい記述**になります。
**その 4 つも、教本の内容と矛盾しない、読んで勉強になる記述にしてください。**

## この試験に効く出題の型

**数字と、似た制度の取り違えで落とす試験なので、次の型を厚めに入れてください。**

1. **数値をそのまま問う** … 「第 2 石油類（水溶性）の指定数量として、正しいものはどれか」。
   **誤答には他の品名の指定数量を置く**
2. **手続の種類を問う** … 「法令上、予防規程について必要な手続として正しいものはどれか」
   （許可・承認・認可・届出・不要 を並べる）
3. **組合せを問う** … 「保安距離を保たなければならない施設の組合せとして正しいものはどれか」。
   **本番で 3 問出た形です**
4. **該当しないものを問う** … 「全 6 類の品名に該当しないものはどれか」。
   **他の類の代表物質を混ぜる**のが定番
5. **向きを逆にした誤答** … 「沸点は外圧が高くなると低くなる」のように、
   **正しい記述の向きだけを裏返す**
6. **物質の性状を問う** … 「◯◯の性状として、誤っているものはどれか」。
   **色・臭い・比重・水溶性・引火点を並べ、1 つだけ裏返す**

> **性状の問題で「無臭」と書くときは注意してください。**
> 全 6 類はほとんどが有臭で、**「無色無臭」は誤答の定番**です。
> **本当に無臭なのはグリセリンとエチレングリコールくらい**です（`docs/primary-numbers.md` B-5）。

## 数字を書くときの決まり

- **`docs/primary-numbers.md` の A 節（法令の値）は断定してよい。**「10 m 以上」と書けます
- **B 節（物性値）は断定しないでください。**出典によって値が違います。
  **「−20 ℃前後」「常温より低い」という向きで問うこと**
- **B 節の表の † は「出典に項目が無い」印です。**その値を問う問題を作らないでください
- **台帳に無い数値を問う問題を作らないでください**

## バックスラッシュの落とし穴

TypeScript の文字列の中に書くので、次のエスケープが要ります。

| 書きたいもの | ファイルに書く形 |
| --- | --- |
| `\n` を文字として出す | `\\n` |
| バッククォート（テンプレートリテラル内） | バックスラッシュ + バッククォート |
| `$` + `{`（テンプレートリテラル内） | バックスラッシュ + `$` + `{` |

**bash のヒアドキュメント（`cat > file <<'EOF'`）は使わないでください。**バックスラッシュが落ちます。
ファイルの編集は、エディタ相当の書き込み（apply_patch 等）で行ってください。

## 報告

最終メッセージに、次を簡潔に書いてください。経過や思考の説明は要りません。

1. 書いた問題数と、id の範囲
2. **正解の位置の分布**（`answer` が 0 / 1 / 2 / 3 / 4 の数）
3. **設問の形の内訳**（「誤っているものを選ぶ」が何問、「正しいものを選ぶ」が何問）
4. **`sectionId` ごとの問題数**（節が偏っていないか分かるように）
5. **`docs/primary-numbers.md` に無いのに問題へ書いた数値**があれば、全部挙げてください。
   **1 つも無いのが正しい状態です**
6. **自信のない問題**（正解が 1 つに定まらないかもしれない、誤答が実は正しいかもしれない、など）が
   あれば、その id と理由。**「自信のない問題はありません」と書かないでください。**
   確信度が低くてもよいので必ず挙げること
7. 教本の記述で、**問題を作っていて誤りではないかと思った箇所**があれば
